---
production:
  adapter: postgresql
  database: app_production
  host: localhost # running app and tests from the host
  username: postgres
  password:
  pool: <%= ENV["DB_POOL"] || 5 %>
  encoding: unicode
